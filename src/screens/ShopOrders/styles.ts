import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  emptyListText: {
    fontSize: 16,
    fontFamily: 'OpenSans-Bold',
    textAlign: 'center',
    marginTop: '50%',
  },
});
